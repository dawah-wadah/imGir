import React from 'react';

export const DeleteButton = ({deleteFunction}) => {
  debugger
  return (
    <button onClick={() => this.deleteFunction()}
      className='deleteButton'>
      Delete
    </button>
  );
};
